<h3>
  <a href="/articles/<%= article.id %>"> <%= article.title %> </a>
</h3>
<small class="text-secondary">Creado por <%= article.user.firstname %> <%= article.user.lastname %> el <%= article.createdAt %></small>

